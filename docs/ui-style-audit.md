# UI 样式核查（2026-09-23）

依据根目录 `DESIGN.md` 和项目 `mbrain-style` skill，检查 `ui/` 下全部七个 Kotlin 文件，包括页面、编辑器、底部弹层、确认对话框及公共组件。

## 统一结果

| 范围 | 处理 |
| --- | --- |
| 首页、能力、设置 | 保留概览卡片与网关主操作；同类入口复用分组行，首组顶部间距保持一致 |
| 工具列表与搜索 | 使用懒加载连续分组，2dp 间隙，仅首尾使用大圆角；搜索结果单项时四角恢复大圆角 |
| 工具详情 | 说明使用 ContentCard，调用名称完整展示、可选择与复制；复制不显示导航箭头 |
| MCP 服务列表、详情 | 连续分组；移除详情装饰图标底框；统一行首图标与导航箭头尺寸 |
| 隧道列表、详情 | 连续分组；运行记录使用标准分组；修正 Token 重启后变化的过时说明 |
| 证书列表 | 分组外形由公共懒加载组件处理，保留每份证书的信息与删除操作 |
| 服务、隧道、证书编辑器 | 统一 EditorSaveBar 的宽度、内边距和按钮高度；标题栏使用页面背景色 |
| 添加、导入、重连操作 | 统一 SecondaryAction 的高度、可选图标尺寸与间距 |
| 下拉选择 | 统一操作行，只在右侧显示下拉箭头 |
| 空状态 | 去掉装饰图标，缩短文案，保留必要操作和错误说明 |
| 外观选择与添加类型弹层 | 统一水平内边距，补齐外观选择的 2dp 间隙；公共分组和间隙透出容器底色，修复弹层中的黑边，已复查明暗主题 |
| 访问凭据 | 保持隐藏 Token，收敛内容内边距，未启动时使用简短提示 |
| 运行记录 | 使用连续分组，保留失败与成功的图标区别 |
| 关于、使用说明、确认对话框 | 核查后保留现有品牌图、提示及原生 Material 操作，不强行改成列表按钮 |

## 验证

- Debug rebuild 与 `:app:lintDebug` 通过；lint 报告为 43 项警告、0 项错误。
- 真机检查工具列表、单项搜索结果、详情，以及浅色/深色和 1.5 倍字体下的工具页、详情页、服务编辑器；字体和主题已恢复。
- 检查 HTTP 与托管进程编辑器、隧道编辑器、证书编辑器、MCP 空状态、外观选择、凭据隐藏、帮助、运行记录、关于页。
- 使用两个临时本机 HTTP 服务检查有数据分组、服务详情、放弃修改与删除确认；临时服务已删除，没有连接外部服务。
- 检查 Root、Shizuku、应用管理、设备信息四种详情，以及一个禁用的临时隧道的列表与详情；临时隧道已删除，未启动 frpc。
- 工具调用名称的复制入口正常触发。最终手机停留在“全部工具”，网关运行中，字体恢复为 1.0，外观恢复为跟随系统。
- 测试截图及构建日志保留在忽略目录 `.local-tools/`，未纳入应用资源。

本轮是样式与交互验证，未验证公网 frps、真实证书导入或每个网络错误状态。证书有数据分支进行了源码检查，未导入测试证书。设备报告输入法已展开，但截图没有可见键盘，因此不将键盘遮挡测试记作通过；保存栏的 IME 避让代码保留，键盘设置已恢复。没有修改 Root/Shizuku 授权开关，也没有重置访问凭据。
