# MBrain UI

Android 原生工具面板，面向管理手机能力与 MCP 服务的用户。每屏帮助用户判断当前状态，并完成一个明确操作。

## 参考与取舍

- RikkaHub 本地源码：分组设置行、图标与文字插槽、独立配置面板、完整的明暗主题。只借鉴设计模式，未复制其组件代码。
- Awesome Design MD / Linear：参考单一强调色、表面层次、细边框、克制动效。该参考是第三方设计分析，不是品牌官方规范。
- Android 官方 Reply 示例：https://github.com/android/compose-samples/tree/main/Reply 。借鉴列表与详情分离。
- 将网页参考适配为 Android：保留浅色主题、中文系统字体、至少 48dp 操作区域；不使用网页营销大标题、专属品牌字体或大段介绍。

## 视觉规则

- 品牌 Logo 使用 `assets/brand/mbrain-logo.svg`：微圆角实心闪电，顺时针倾斜 9°，石墨灰正面、深色薄侧面和轻投影。桌面与关于页使用立体版；通知和系统主题图标使用同轮廓的单色版，不加入彩色装饰。
- 主色 #5E6AD2；浅色画布 #F3F5FA，深色画布 #111216；深色一级表面 #1B1D24。
- 浅色正文 #1B2435；深色正文 #E3E8F2。成功使用语义青绿，错误使用语义红。
- 标题使用系统 sans 的 Medium/Semibold；正文用系统 sans Regular；Token、命令结果用 Monospace。
- 文字层级采用 Material typography；设置行正文 bodyLarge，辅助文字 bodyMedium；组标题 titleSmall/Semibold 并使用主题色，页面重点 headlineSmall。
- 页面边距 16dp，横条占满内容宽度，平板内容最大 720dp；行内边距 16dp；长列表使用 LazyColumn。
- 设置分组参考 RikkaHub：首尾外圆角 20dp、组内圆角 4dp、横条间距 2dp，以表面色区分而不加边框。组标题左缩进 4dp，距下方内容 8dp；行首使用无底框的 24dp 图标。单行最小高度 56dp，双行最小高度 72dp，支持字体放大。
- 首页取消连接示意图和装饰图标；网关启停使用青绿色，工具与服务概览分别使用浅蓝和浅绿表面，深色模式使用对应暗色表面。
- 页面切换使用短淡入淡出；原生组件保留按压、水波纹、禁用与焦点反馈。

## 交互规则

- 底部导航只用于四个主页面；详情页带返回按钮，系统返回与页面返回一致。
- 首页仅展示顶部能力概览（工具/服务）和服务网关状态；网关卡片内使用 64dp 开关按钮，播放/停止图标对应状态，处理中显示进度并禁用按钮。首页不显示连接组件、说明文字或重复的顶栏状态；连接凭据和运行记录通过设置进入，错误通过 Snackbar 反馈。能力管理通过底栏进入。
- Root / Shizuku 详情只保留一个启用开关和工具入口。打开时自动检查并申请权限，授权成功才启用；拒绝、超时或服务未启动保持关闭并显示原因。权限流程不依赖 Activity 生命周期；已启用 Root 在进程启动时自动恢复检查，工具仅在权限可用时发布。不提供独立授权入口或手动身份验证。
- MCP 列表进入服务详情；添加先选择类型，再进入全屏表单。托管进程参数逐项填写，不要求 JSON。
- 添加类型仅包含 HTTP 服务与托管进程；名称和地址留空由用户填写，不内置 MT 或其他特定服务商入口。已保存连接按用户配置展示。
- 编辑页底部固定保存；未保存离开提示放弃修改。移除服务确认；连接失败显示在对应服务详情并提供重试。
- 工具列表只显示名称与来源；完整说明进入工具详情。搜索同时匹配中文名称、原始名称和描述。
- Token 默认隐藏，复制有反馈。运行记录在独立页，最多 40 条。
- 各主页面和详情保存滚动位置；编辑草稿与导航在 Activity 重建时恢复。

## 实现入口

- DesignSystem.kt：主题和通用组件。
- MBrainApp.kt：页面栈、导航、反馈、弹层。
- OverviewScreens.kt：首页、能力、设置、记录。
- ConnectionScreens.kt：MCP 列表、详情、完整编辑页。
- DetailScreens.kt：工具、凭据。
